# FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# Database
aiosqlite>=0.20.0

# Validation and serialization
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Utilities
python-dateutil>=2.9.0

# Authentication (optional - only needed if NODES_AUTH_ENABLED=true)
PyJWT>=2.8.0

# Rate limiting (optional - only needed if auth enabled)
slowapi>=0.1.9

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
